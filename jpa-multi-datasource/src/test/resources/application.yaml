spring:
  application:
    name: jpa-multi-datasource-test
  profiles:
    active: firstly, secondly
  jpa:
    hibernate:
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy